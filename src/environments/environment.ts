// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: true,
  _accountSheetApi_url: 'http://localhost:8082/api'
};

/*
 * For easier debugging in development mode, you can upload_Old the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This upload_Old should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// upload_Old 'zone.js/dist/zone-error';  // Included with Angular CLI.
